package gmdebug.lua.debugee;

import gmdebug.lua.debugee.managers.VariableManager;
import gmdebug.lua.debugee.handlers.HVariables;

class Dumper {
	public static function dump() {
		// final vars = new HVariables(new VariableManager());
	}
}
